#include <stdio.h>

int main() {
  int num1, num2, soma;

  // Leitura dos dois números
  scanf("%d %d", &num1, &num2);

  // Cálculo da soma
  soma = num1 + num2;

  // Impressão da soma
  printf("%d\n", soma);

  return 0;
}
